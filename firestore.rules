rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users can only read and write their own user document
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Global canvas document - authenticated users can read and write
    match /globalCanvas/{document=**} {
      allow read, write: if request.auth != null;
    }
    
    // Canvas objects - authenticated users can create, read, update, delete
    match /canvasObjects/{objectId} {
      allow read, write, create, delete: if request.auth != null;
      
      // Ensure createdBy and lastModifiedBy fields are set correctly
      allow create: if request.auth != null 
        && request.resource.data.createdBy == request.auth.uid
        && request.resource.data.lastModifiedBy == request.auth.uid;
        
      allow update: if request.auth != null
        && request.resource.data.lastModifiedBy == request.auth.uid;
    }
    
    // Deny all other access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
